import { TextFieldPrefixDirective } from './text-field-prefix.directive';

describe('TextFieldPrefixDirective', () => {
  it('should create an instance', () => {
    const directive = new TextFieldPrefixDirective();
    expect(directive).toBeTruthy();
  });
});
