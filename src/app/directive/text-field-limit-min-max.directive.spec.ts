import { TextFieldLimitMinMaxDirective } from './text-field-limit-min-max.directive';

describe('TextFieldLimitMinMaxDirective', () => {
  it('should create an instance', () => {
    const directive = new TextFieldLimitMinMaxDirective();
    expect(directive).toBeTruthy();
  });
});
